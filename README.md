# applied-ai-service

Minimal starter repository for the Applied AI Service project.

This repository contains a small Python package scaffold and an example entrypoint.

## Quick start

- Create a virtual environment
- Install dependencies from `requirements.txt`
- Run `python -m applied_ai_service.app` to run the example
